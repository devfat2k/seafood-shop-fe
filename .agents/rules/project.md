---
trigger: always_on
---

# Project source of truth

Always follow `./AGENTS.md` as the source of truth for this project: stack, commands, API contract, UI rules, architecture, quality gates, and boundaries.

Before implementing any feature, read the relevant spec in `docs/specs/` and the matching convention in `docs/conventions/`.
